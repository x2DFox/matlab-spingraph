# MATLAB Spingraph

![result](https://github.com/x2DFox/matlab-spingraph/blob/main/ambiguity.gif)

Функция `spingraph.m` для MATLAB создает анимированный GIF-файл с вращением текущего 3D-графика. Используется только начальный инструментарий MATLAB, а значит - нет зависимостей от тулбоксов. Модуль также комплектуется `demo_ambiguity.m`, которая строит функцию неопределенности прямоугольного импульса и демонстрирует его работу.

# Установка

Скопируйте `spingraph.m` в папку с вашим проектом или добавьте его в путь MATLAB

# Справка

## Синтаксис

```
spingraph(filename, angles, delay, elev1, elev2, elev3)
```

## Параметры

```
filename - имя файла, например, 'name.gif'
    !!! Если файл с таким именем уже существует, то функция его перезапишет
angles:
    горизонтальный массив углов в градусах, например 0:10:360, где
    0   - Начальное значение (с какого угла начать)
    10  - Шаг (через сколько градусов делать кадр)
    360 - Конечное значение (каким уголом закончить)
delay - задержка между кадрами в секундах, например 0.5
elev1 - вертикальный угол в градусах для 1-го вращения (по умолчанию = 30)
elev2 - вертикальный угол в градусах для 2-го вращения (опционально)
elev3 - вертикальный угол в градусах для 3-го вращения (опционально)
```

## Примеры

```
spingraph('name.gif', 0:30:180, 1.0)
spingraph('name.gif', 90:10:360, 0.05, 30, 75)
spingraph('name.gif', 0:1:90, 0.1, 30, 75, 90)
```

# Дополнительно

Функция также поддерживает вывод справки прямо внутри MATLAB Command Window:

```MATLAB
help spingraph
```
